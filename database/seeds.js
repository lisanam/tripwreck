const Collections = require('./collections');
const Models = require('./models');

const initalize = {
  user: () => {
  },
  list: () => {

  },
  store: () => {

  },
  type: () => {
  },
  category: () => {

  }
}

initalize.user();
initalize.type();
initalize.category();
initalize.store();
initalize.list();